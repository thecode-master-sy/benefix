import { SafeAreaView } from "react-native-safe-area-context";
import { Text } from "react-native";

export default function RegisterPage() {
	return (
		<SafeAreaView>
			<Text>this is the register page for benefix</Text>
		</SafeAreaView>
	);
}
